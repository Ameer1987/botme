{% extends 'layout/layout.html.twig' %}

{% block body %}
    {% if cart and cart.items|length %}
        <h1>{{ cart.Type }} Cart</h1>
        <table class="table">
            <thead>
            <tr>
                <th>Code</th>
                <th>Name</th>
                <th>Unit price</th>
                <th>Quantity</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for item in cart.items %}
                <tr>
                    <td>{{ item.product.code }}</td>
                    <td>{{ item.product.name }}</td>
                    <td>{{ item.unitPrice }}</td>
                    <td>{{ item.quantity }}</td>
                    <td><a href="{{ path('cart_delete_item',{'id': item.id}) }}">Delete</a></td>
                </tr>
            {% endfor %}

            </tbody>
        </table>
        <br/>
        <ul>
            <li>
                <a href="{{ path('cart_edit_items',{'id': cart.id}) }}">Edit cart</a>
            </li>
            <li>
                <a href="{{ path('cart_clear',{'id': cart.id}) }}">Clear cart</a>
            </li>
        </ul>
    {% else %}
        <h1>Cart</h1>
        <div>No cart found</div>
        <br/>
    {% endif %}

    <ul>
        <li>
            <a href="{{ path('homepage') }}">Back to home</a>
        </li>
    </ul>
{% endblock %}
